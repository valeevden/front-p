// var is obsolete
var i = 40;
// let is common way to set variable
let ii = 41;
// const - unchangeable variable. constant
const iii = 43;
console.log(i);

const cat = {
	name: 'NyanCat',
	age: 7,
	breed: 'Russian',
}

function printCatInfo(cat) {
	console.log(cat.name + ' ' + cat.age + ' ' + cat.breed);
}

printCatInfo(cat);

const cat
